# AI_HANDOFF.md — Echarpe / Glow Retail System

> المرجع الوحيد للحالة. يتحدّث مع كل ميزة/إصلاح/تغيير في قاعدة البيانات.
> **آخر تحديث: 19 سبتمبر 2026** (الكوتة · إنستاباي · إصلاحات الفلوس · المرتجع
> بالرصيد · باج firebase-admin v14 · العيب المحاسبي المفتوح 4هـ).
> المالك: **محمود** (سلسلة ريتيل: طرح، بيچامات، ملابس منزلية). عربي مصري.
> كود كامل شغّال خطوة خطوة، أقل أسئلة، **متختبر بالـ harness قبل التسليم**.

---

## 0. قواعد التعامل (مهمة — اتفقنا عليها)
- **الملفات تتسلّم بأسمائها الحقيقية دايمًا.** ممنوع أطلب منه يغيّر اسم ملف.
  لو فيه ملفين بنفس الاسم (`sw.js` بتاع sales و`sw.js` بتاع POS) → **رسالتين منفصلتين**.
- **الملف يتسلّم كامل**، مش أجزاء يلصقها بنفسه.
- الاختبار بالـ harness **قبل** التسليم + **اختبار سلبي**: رجّع الباج واتأكد إن الاختبار بيقع.
- الرفع: **دفعة واحدة ← علامة خضراء ← الدفعة اللي بعدها**. وبعد الرفع افتح/اقفل مرتين.
- **كل تعديل يرفع `CACHE_NAME` في `sw.js`** وإلا الجهاز يفضل على القديم.
- ⚠️ قبل تسليم أي ملف، اتأكد إنه أحدث نسخة (grep على ميزة معروفة فيه) —
  حصل مرتين إن ملف من فرع أقدم كان هيلغي إصلاحات، والاختبارات هي اللي مسكته.
- **GitHub هو المرجع** (قرار المالك) — مش نسخ الفولدرات المحلية.
- ⚠️ **أي اختبار بيمنع كلمة (مثلاً `prompt` أو `CDN`) لازم يشيل التعليقات
  الأول.** وقعنا في ده مرتين في يوم واحد: الاختبار مسك الكلمة جوّه تعليق
  بيشرح المنع نفسه، وطلّع فشل وهمي شكله باج حقيقي.
- ⚠️ **ممنوع `prompt()` و`confirm()` و`alert()` في POS** — Electron مبيدعمهمش،
  والديالوج بيطلع بشكل ويندوز وحش والـprompt بيفشل بصمت. البديل مودال داخلي.
  فيه اختبار بيمنعهم في `instapay-pos.js`.
- ⚠️ **الحقن في شاشة مش بتاعتك = اقرا هيكلها الأول.** كارت إنستاباي اتحقن في
  **جذر** `#rolesScreen` (عمود flex: عنوان + صندوق `flex:1; overflow-y:auto`)
  فعصر صندوق الأقسام لارتفاع صفر — الكونسول يقول "ظاهر" والشاشة فاضية. ضاعت
  ساعات في تخمينات (جلسة أدمن · طي · ترتيب). الصح: الحقن **جوّه** صندوق الإسكرول،
  واختبار jsdom على الهيكل الحقيقي (`test-settings-placement.js`) مش فحص نصوص.
- ⚠️ **لما الدليل يناقض كلام المالك، الدليل ناقص مش المالك غلطان.** اطلب فيديو/
  اقرا الكود قبل التخمين التالت.
- ⚠️ **ميزة عرض عمرها ما تخفي محتوى افتراضيًا** (الطي بالضغط بس).
- ⚠️ **الميزة الجديدة = طبقة فوق، مش تعديل جوّه.** إنستاباي اتربط بـ POS
  بالتغليف (monkey-patch) على `confirmPayAmount` و`confirmPayment` من غير ما
  يتلمس `pos-sale.js` ولا `pos-admin.js` — لأن دول آلاف السطور فلوس متختبرة.
- ⚠️ اختبار بيمسك بلوك بـ regex لازم يتأكد الأول إن البلوك اتلقى — regex اتكسر مرة
  وطلّع 13 فشل وهمي شكلهم باجات حقيقية. استخدم استخراج بالأقواس المتوازنة (extractFn).

---

## 1. الإصدارات الحالية
| التطبيق | الإصدار |
|---|---|
| POS (`pos/sw.js`) | **v694** |
| sales (الحضور/HR) | v618 |
| feedback (تابلت الفرع) | **v691** |
| loyalty / glow | **v690** / v75 |
| echarpe office | v5 |
| Cloud Functions | **29 دالة منشورة** (كانت 62) |
| اختبارات إنستاباي والتجربة | ~140 ناجح · 0 فاشل (ملفات مستقلة) |
| `node tests/run.js` | ⚠️ لسه فيه فشل من أرقام إصدارات ChatGPT — محتاج تنضيف |

> ⚠️ الأرقام دي من جلسة 18–19 سبتمبر. المرجع **GitHub** مش النسخ المحلية.

---

## 1أ. 🔥 كارثة 18 سبتمبر — الكوتة (اقرا ده قبل أي deploy)
ChatGPT ضاف منظومة finance/InstaPay في **29 دالة** (13 في `instaEvidence.js`
+ 16 في `financeCheckout.js`). العدد وصل **62 دالة** في `us-central1` وعدّى
حد الـCPU للمشروع، فالنشر فشل بـ:
`Quota exceeded for total allowable CPU per project per region` (ظهر 120 مرة).
**النتيجة:** 30 دالة فشلت تتحدّث ووقعت — منهم `onlineOrderPlace` و`paymobWebhook`
و`rateVisitPush` و`hijabTryOn` و`creditSpend`. الفرع وقع بالكامل.
- ✅ **اتحل:** اتمسحت الـ29 دالة، والعدد بقى 29، والأساسي رجع.
- 🔴 **القاعدة:** كل دالة = خدمة Cloud Run بتاخد من نفس الحصة. **العدد نفسه
  هو المشكلة مش الكود.** أي ميزة جديدة = دالة واحدة بـ`action` جواها، مش
  دالة لكل أمر. إنستاباي كله اتعمل في **دالتين**.
- `index.js` فيه السطرين المعطّلين بـ`/* */` لـ`financeCheckout` و`instaEvidence`
  — **ممنوع يترجّعوا**.

## 1ب. 📱 إنستاباي على تابلت الفرع (شغّال)
الكاشير تختار «انستا باي» → الطلب يروح لتابلت الـfeedback → العميلة تشوف QR
والمبلغ → تدوس «تم التحويل» → الكاميرا الأمامية تمسح الإيصال → السيرفر يقرا
بـCloud Vision ويطابق **5 حقول**، وبعدها الفاتورة تتحفظ وتتطبع.
- **الدوال:** `instaPay` (start/status/ready/cancel/approveManual/finalize) ·
  `instaScan` (صورة → Vision → مطابقة). **دالتين بس — ممنوع تتقسم.**
- **الملفات:** `functions/instapay.js` · `functions/instapayCore.js` (منطق خام
  متختبر من غير سيرفر) · `feedback/instapay-tablet.js` · `pos/instapay-pos.js`.
- **المطابقة:** الإيصال مش فاشل · المبلغ بالقروش · الوقت جوّه النافذة
  **في الاتجاهين** · المستفيد في ناحية «إلى» · رقم مرجعي واضح.
- 🧩 **تجميع الحقول عبر الفريمات:** الحقل اللي اتقرا بيتقفل ومبيترجعش يتقرا.
  التجميع **مربوط بنفس الرقم المرجعي** — رقم مختلف يمسح كل اللي اتجمّع
  (وإلا حد يجمّع مبلغ من إيصال ورقم من إيصال تاني).
- 🟢 **النجاح = غياب كلمات الفشل**، مش وجود «تم التحويل بنجاح». العبارة دي
  أرفع سطر في الإيصال وبتضيع في القراءة، وكانت بتقفل الاعتماد 3 دقايق.
- 🔒 `payments.instapay` موجود أصلًا في التقفيل. الرقم المرجعي بيتحجز في
  `instapay_references` بعد الحفظ فنفس الإيصال ميعدّيش مرتين.
- 🔴 **البوابة:** فاتورة فيها إنستاباي متأكدش **مش بتتحفظ**. أهم سطر في النظام.
- ✍️ **تأكيد يدوي** للطوارئ: بيتسجّل باسم الكاشير و`mode:'manual'` و
  `bankState:'PENDING_BANK_RECONCILIATION'`.
- 💰 **التكلفة:** Vision أول 1000 صورة/شهر ببلاش وبعدها ~7 قروش للصورة.
  سقف شهري في الإعدادات (افتراضي 1000) — لما يخلص إنستاباي **مبيقفش**،
  بيكمّل تأكيد يدوي. وسقف 8 فريمات للطلب.
- ⚙️ **الإعدادات لكل فرع** في شاشة **الصلاحيات**: تشغيل/قفل · العنوان ·
  عناوين إضافية (رقم محفظة) · اسم المستفيد · نافذة الوقت · السقف · الـQR.
- 🔄 التابلت بيكتشف لوحده لو الكاميرا بتعكس الصورة وبيحفظ ده للجهاز.
- 🩺 تشخيص: `instaDiag()` و`instaText()` من كونسول POS.

## 1ج. 📲 دعوة تحميل التطبيق (`feedback/app-invite.js`)
بتظهر بعد ما العميلة تكتب رقمها وتشوف الترحيب. QR **متولّد مسبقًا ومدفون في
الملف** كـSVG (مفيش مكتبة ولا إنترنت). بتتخفي لو مستند العميلة فيه
`fcmTokenAt` — دليل ناقص (اللي رفضت الإشعارات هتشوفها) والخطأ في الاتجاه الآمن.
مبتظهرش فوق إنستاباي.

## 2. المعمار
- Vanilla JS، مفيش build. Firebase compat (POS/apps) + modular (sales).
- Firestore: `customer-feedback-8ac1d` · offline persistence شغّالة.
- الاستضافة: GitHub Pages — رفع يدوي. الدومين: `echarpe.store/pos`.
- **Cloud Functions** من `C:\Users\Mahmo\echarpe-push`:
  `cd echarpe-push && firebase deploy --only functions`
- **Electron POS** من `C:\Users\Mahmo\Documents\pos-desktop` → `npm run build`
  → `dist\echarpe POS Setup 1.0.0.exe`.
- `security/firestore-phase2.rules` — بتتنشر من Console يدوي.
- **قرار المالك: تطبيق sales عمره ما هيشتغل ويندوز/Electron** — فالـ4 `prompt()`
  الموجودين فيه مش مشكلة ومتفتحش الموضوع تاني. المنع خاص بملفات POS بس.

---

## 3. الملفات
- `pos/` — + `instapay-pos.js` (جديد) · `credit-core.js` · `credit-ui.js`
  · `index.html` · `pos-core.js` · `pos-sale.js` · `pos-admin.js` · `pos-reports.js`
  · `app.js` · `frames.js` · `import.js` · `products.js` · `transfers.js` · `chat.js`
  · `staff.js` · `profiles.js` · `loyalty.js` · `search.js` · `discounts.js` · `ui-editor.js` + `sw.js`
- `sales/` — `index.html` + `sales-app.js` (module) + `sales-ui.js` (classic) + `sw.js`
- `feedback/` — `index.html` + `instapay-tablet.js` + `app-invite.js` + `sw.js`
- `tryon/` — `photo-core.js` (قص الشبكة) + باقي ملفات التجربة
- `Office/` — `index.html` · `office.js` · `sw.js`
- `tests/` — `run.js` + `helpers/` + 9 ملفات اختبار
- `docs/` — `DEVELOPER.md` · `OPERATIONS.md`

---

## 4. 💰 جلسة إصلاحات الفلوس (الأحدث — مرجع أساسي)

### 4أ. POS: المبيعات والتقفيل — الباجات اللي كانت بتعمل عجز/أوفر يومي
1. **الفكة كانت بتتسجل مدفوعات:** الكاشير يكتب المستلم (600 لفاتورة 500) →
   `payments.cash=600`. اتصلحت بـ`normalizePayments`: المحفوظ = المطبّق،
   والمستلم/الفكة في `cashReceived`/`changeGiven`، والفاتورة المطبوعة بتاخد المستلم.
2. **فاتورة العكس كانت من غير payments** → كاش/فيزا التقفيل مش بيتصفّروا.
   دلوقتي بتحمل مدفوعات سالبة بنفس طرق الأصلية.
3. **التقارير كانت بتخصم العكس مرتين** (الأصلية مستبعدة + السالبة محسوبة).
   `repAggregate` بيستبعد الطرفين مع بعض. التقفيل (`dcAggregate`) بيشملهم مع بعض.
4. **"النهاردة" اتوحدت على يوم الشغل** (bizDayStartMs) في التقارير والتقفيل —
   كان فيه فاتورة فجر بتظهر في شاشة ومتظهرش في التانية.
5. **فواتير ما بعد آخر تقفيل** (السبب الأول للأوفر المزمن): بتتكشف تلقائي —
   بانر برتقالي + سطر "الفرق الحقيقي بعد استبعادها" + بتتسجل في سجل التقفيل.
6. **ثغرات التقفيل اتقفلت:** استعلام بنطاق زمني بدل limit(300)/limit(1500)
   (فولباك للطريقة القديمة لو الاستعلام اترفض) · فواتير الأوفلاين بطابع محلي
   `createdAtMs` + دالة `saleTs` (سيرفر لو اتأكد وإلا محلي) + استعلام تكميلي ·
   بانر أحمر + **تأكيد إجباري** لو التقفيل والنت قاطع (fromCache) · عدّاد
   الفواتير المعلقة. الحالة بتتسجل في سجل التقفيل (`pendingCount`/`closedFromCache`).
7. **🎁 ثغرة النقط→كاش (خطيرة، اتقفلت):** الاستبدال كان محدود بالرصيد بس —
   سلة 50ج + استبدال 100ج = إجمالي سالب = "مرتجع" بيطلّع كاش. 4 طبقات:
   `_redeemMaxUnits` (سقف بالرصيد وقيمة الفاتورة) في المسارين + تنضيف تلقائي
   عند حذف منتج + حارس نهائي يرفض فاتورة سالبة فيها استبدال. والخصم اليدوي
   بقى ميلمسش سطور الاستبدال/المكافأة.
- ملحوظة انتقالية: الفواتير القديمة متسجلة بالفكة جواها — تقارير الفترات
  القديمة هتفضل شايلة الفرق، الجديد هو الصحيح.
- استعلام النطاق بيستخدم الـ index المركب الموجود (branch + createdAt) —
  لو ظهر لينك "create index" في الكونسول يتعمل مرة واحدة، والفولباك شغال لحدها.

### 4ب. sales: الرواتب — المحرك كان مبني ومش موصّل
**الاكتشاف:** `lateHoursFrom` · `swapHoursFrom` · `monthlyTimeSummary` كانوا
موجودين ومتختبرين و**محدش بيناديهم في الإنتاج** — اللوحة تقول "يومين خصم"
والمرتب يتصرف كامل.
1. **رصيد الوقت بيتخصم من المرتب:** computeSalary بيجمع الساعات غير المعذورة
   في الفترة → أيام (بسقف maxDaysPerMonth) → خصم بقيمة اليوم. سطر في اللوحة والإيصال.
2. **التأخير بيسجل ساعات رصيد** (10 دق = ساعة، بسماح lateGraceMin وسقف يومي)
   بدل الغرامة الثابتة القديمة — كانت بتتسجل في كشف مش بيتخصم أصلًا، وبوابة
   الالتزام 90% كانت شغالة من غير التأخير.
3. **الخصومات الإدارية (كشف الخصومات) بتتخصم فعليًا** — كانت display-only.
4. **ثغرة يوم 31:** فترة المرتب 1→30 (قرار المالك) لكن السلف دين — نافذتها
   بتمتد لآخر اليوم التقويمي في الحسبة الكاملة (التصفيات المقطوعة زي ما هي).
5. **النقط الكسرية موحّدة على `sumPoints`** (الوزن) في الإيصال وحالة السباق —
   كانوا بيعدّوا مستندات بينما لوحة العمولات بتجمع أوزان.
6. **سقف السلف بمفتاح شهر محلي** بدل UTC (سلفة فجر يوم 1 كانت بتتحسب ع الشهر اللي فات).
7. **التبديل اليدوي بيسجل ساعات** بقاعدة "الأول مجاني + swapHours بعده" بدل غرامة ثابتة.
8. دوال المحرك كلها اتعرّضت على window (القاعدة الذهبية — swapHoursFrom كانت هتقع بصمت).
- **انتقالي:** غرامات تأخير ثابتة متسجلة في شهر التحديث هتتخصم (بند إدارية)
  والجديدة ساعات — لو ازدواج لنفس الأيام، الأدمن يمسح القديمة (زرار ✖).
- **قرار مأجل للمالك:** زرار "خصم 50ج" على الغياب بيضيف فوق خصم اليوم التلقائي
  (إضافة مش بديل) — اتساب كده عمدًا لحد ما يقرر.

---

### 4ج. إصلاحات 19 سبتمبر (تلات باجات كانوا شغالين في الإنتاج)
1. **💳 الرصيد ماكانش بيتحفظ.** في `creditAdjust` كان فيه متغيّر `isChange`
   بيحدد إن دي «باقي فاتورة» والتعليق فوقه بيقول إنها تعدّي من غير موافقة —
   **والمتغيّر ماكانش مستخدم في أي شرط**. فكل إضافة من الكاشير كانت بتروح
   لطابور موافقات محدش فاتحه. الكاشير تقول «اتحفظ» والرصيد مبيزيدش.
   الإصلاح: `if (who.role !== 'owner' && !isChange)`.
2. **↩️🎁 نقط المرتجع باسم العميلة ماكانتش بتتخصم.** `_retPointsDeduct`
   بيتحسب بس للسطور اللي فيها `fromInvoice`، والمرتجع باسم العميلة مفيهوش
   ربط. و`Math.max(0, loyaltyPointsEarned)` بيرمي النقط السالبة.
   **النتيجة: البضاعة ترجع والنقط تفضل — نقط بتتولد من العدم.**
   الإصلاح: `_unlinkedDeduct` بنفس معادلة الكسب (`floor(قيمة/_rate)`).
3. **📡 الفاتورة ماكانتش بتظهر لحظيًا عند العميلة.** تبويب الفواتير في
   `loyalty/index.html` كان `get()` مرة واحدة و`tabStarted` بيمنع أي قراءة
   تانية. بقى `onSnapshot`. العروض والمنتجات سايبينهم `get()` عن قصد.
⚠️ **دي أول مرة اتلمس `pos-sale.js`** (12 سطر في مكان واحد) — اختبار بيتأكد.

### 4د. 💳 المرتجع بأربع طرق — ✅ اتعمل واتجرّب (19 سبتمبر)
«رصيد العميلة» بقت **طريقة دفع عادية** (`payments.credit`) بتظهر في المرتجع بس
(زرار `pmCredit` + `pos/refund-credit.js` بالتغليف). الرصيد بيتكتب **بعد** الحفظ
عن طريق `creditAdjust` بمصدر `refund` (بيعدّي من غير موافقة زي `change` — لازم
رقم فاتورة) ومعاه `idem` = `refund:<فاتورة>:<رقم>:<مبلغ>`.
- **التقفيل:** `dcAggregate` بيرجّع `creditSales`، والتسوية بتجمعه
  (`+ salary + creditOut`). `payments.credit` سالب في المرتجع فبيعادل نزول
  `systemTotal`. **اختبار سلبي** بيثبت إن المعادلة القديمة بتطلّع أوفر 350.
- `dcAggregate` بيجمع دلوقتي 5 مفاتيح: `cash · visa · instapay · salary · credit`.
  **أي طريقة دفع جديدة لازم تتضاف هناك وفي التسوية.**

### 4هـ. 🔴 عيب محاسبي مفتوح — الخصومات كسطور سالبة (أهم اكتشاف 19 سبتمبر)
صرف الرصيد · استبدال النقط · المكافأة · كارت الهدية — كلهم بيتسجّلوا **سطر سالب
جوّه الفاتورة** مش طريقة دفع. فاتورة 350 مدفوعة كلها من الرصيد = `total: 0`.
- **التقفيل سليم** (الإجمالي وطرق الدفع بينزلوا مع بعض) — عشان كده محدش لاحظ.
- **رقم المبيعات غلط:** مرتجع 350 بالرصيد ثم بيع 350 بنفس الرصيد = السيستم
  بيقول **−350** والحقيقة صفر. (المالك شافها: −375 بدل −25.)
  بيأثر على: مبيعات الفرع · تارجت الشيفت · نقطة البياعة · متوسط الفاتورة.
- **الحل المتفق على اتجاهه:** أي حاجة بتقلّل المطلوب من العميلة تبقى طريقة دفع:
  `payments.credit` (+) · `payments.points` · `payments.reward` · `payments.giftcard`
  — وكلهم يتضافوا في `dcAggregate` والتسوية زي `credit`.
- ⚠️ **قرار مطلوب من المالك قبل التنفيذ:** لما الفاتورة تفضل بقيمتها الكاملة،
  العميلة هتكسب نقط عليها. المقترح: تكسب لو الرصيد من كارت هدية (فلوس حقيقية)،
  ومتكسبش لو من مرتجع. الدفتر عارف مصدر كل حركة.
- ⚠️ تغيير كبير: بيلمس حساب النقط ونقطة البياعة والتقارير والفاتورة المطبوعة.
  **يتعمل في جلسة لوحده بكونتكست كامل، مش في آخر جلسة طويلة.**

### 4و. 🔥 `admin.firestore is not a function` (اتصلح 19 سبتمبر)
`giftCredit.js` و`goldPriceUpdate.js` كانوا على `require('firebase-admin')` +
`admin.firestore()`، والمشروع على **firebase-admin v14** اللي شالت الشكل ده.
**كل دوال الرصيد وكروت الهدايا و`staffAccessSet` كانت بتقع في كل نداء**، والخطأ
بيوصل العميل كـ`INTERNAL`. و`goldPriceUpdate` مجدولة فكانت بتقع كل يوم بصمت.
- كانت مستخبية ورا باجين تانيين (الـ`throw` في `creditSpend` + طابور الموافقات).
- السبب الجذري: `^` في `package.json` → اتثبتت النسخ من غير `^`.
- **`functions/preflight.js`** — يتشغّل قبل أي deploy: استيراد قديم · نسخ بـ`^` ·
  عدد الدوال مقابل حد الـCPU · سينتاكس. **`npm ci` مش `npm install`.**
- `MONITORING-SETUP.md`: تنبيهين Log-based (عام + الدوال المجدولة من أول خطأ).
  **لسه المالك ماعملهمش.**
- ⚠️ `npm audit fix --force` ممنوع — بيرقّي بالعافية وبيكسر التثبيت.

## 5. 💳 Paymob — مكتمل وشغّال
`paymobTerminalOrder` (ترنين الماكينة + تسجيل pending في `pos_paymob_txns`) ·
`paymobWebhook` (HMAC-SHA512 على 20 حقل + timingSafeEqual، بيرد 200 في الأخطاء).
الأسرار في Secret Manager. Webhook URL في كل integration:
`https://us-central1-customer-feedback-8ac1d.cloudfunctions.net/paymobWebhook`
- Integration التجربة: 5228563 (Glow) · Auto callback مفعّل.
- ⚠️ "You haven't set up a POS" تحت MID 629614 — لسه بيتحسم مع Paymob لباقي الفروع.
- الإعداد: `pos_test_settings/paymob` { enabled, autoPrint, terminalIdByBranch }.
- الطباعة التلقائية بشروطها الأربعة + `payDiag()` + شاشة الإلغاء الإجبارية
  (Paymob مفيهاش سحب طلب من الماكينة) — القفل مربوط بـ visaStillSelected.

## 6. 🖨️ الفاتورة والدرج
- بلوك الكارت (إنجليزي dir=ltr) بشرط `payments.visa > 0` · عنصر `cardTxn` في محرر التصميم.
- شاشة المرتجع: بانر الفرع + نسخ رقم العملية.
- **💰 الدرج معلّق في فرع echarpe:** `rawToPrinter` خام عبر winspool (PowerShell+C#)،
  4 أوامر ESC/POS + Star (TSP100 مش بتفهم ESC/POS — أرجح سبب إن Glow شغال وecharpe لأ).
  **[PENDING]** بناء exe بآخر main.js وقراءة `drawerError`.

## 7. 🕕 يوم الشغل
`bizDayStartMs/bizDayKey/isSameBizDay` في pos-core · الساعة الفاصلة من
`pos_test_settings/day_cfg.startHour` (افتراضي 6) · مربوطة دلوقتي في: التقفيل
والتقارير والمرتجع وترند الأيام. الفواتير القديمة بتاريخها التقويمي (قرار المالك).

## 8. 📦 الاستيراد (QuickBooks)
Excel/CSV بـ SheetJS · ربط Item Name/Number/Regular Price/Average Unit Cost/Qty 1 ·
مسح المخزون بتأكيد كتابة "مسح" · خصم المباع بعد وقت التصدير بمعاينة.
**[PENDING]** استيراد ملف echarpe الفعلي ثم **جرد فعلي**.
**⚠️ معماري مفتوح:** الباركود = مفتاح المستند → البراندين بيكتبوا على بعض.
لو Glow هتشتغل جنب echarpe لازم فصل المخزون (doc id مسبوق بالبراند).

## 9. 🔍 المسح والباركود
الأصفار البادئة (مطابقة تامة ثم بعد الشيل، ولو أكتر من تطابق مبيخمّنش) ·
الكيبورد العربي (`fixArabicKeyboard` بس لو الناتج شكل كود معروف FT/ECH/GLW/كارت).

## 10. ⚠️ ممنوع `prompt()` في POS بس — Electron مش بيدعمها
البديل `askText()`. `tests` فيها فحص المنع (في نسخة الاختبارات المحلية القديمة —
الفحص ده من الـ21 ملف اللي ماترفعوش، يتعاد بناؤه لو احتجناه). sales معفي (قرار المالك).

## 11. 🔐 أدوار sales
مالك 2005 · مدير من `pos_test_settings/sales_roles.managerCode`. المدير:
approvals/day/tasks/orders · محجوب: money/settings/terminate/people.
`roleCan` فيه حارس `adminUnlocked` (جلسة مقفولة = صفر صلاحيات — عليه 11 تأكيد).
"إعدادات السلف" perm=`settings` (لوحة إعدادات مش عرض فلوس — محجوبة عن المدير برضه).
تشخيص: `roleDiag()`.

## 12–14. شريط "محتاج منك" · الإطارات وتارجت الشيفت · echarpe office
(زي ما هم — مفيش تغيير في الجلسة الأخيرة غير إن اختباراتهم اتصلحت.)
office: قرار محاسبي مثبت — السلف مش بند خصم إضافي.
**⚠️ تحصين office مأجل بقرار المالك:** باسورد الفرع متحفظ نص صريح في
localStorage + كود المالك علامة '1' قابلة للزرع — خطرها بس مع مسك الجهاز
فعليًا. الخيارات المعروضة: سيبها / إدخال كل مرة / PIN سريع. **لسه ماختارش.**

## 15. المجموعات
زي القديم + حقول جديدة: `pos_test_sales`: `cashReceived` · `changeGiven` ·
`createdAtMs` (طابع محلي للأوفلاين) · فاتورة العكس فيها `payments` سالبة.
سجل التقفيل `dayclose_<branch>_<date>`: + `overShortReal` · `lateCash/Total/Count`
· `pendingCount` · `closedFromCache`. `sales_time_credit` بقى فيه type:'late' و'swap'.

## 16. الدوال المنشورة (29 — كانت 62)
**us-central1 (17):** `instaPay` · `instaScan` · `creditSpend` · `creditAdjust`
· `creditKeepChange` · `creditRequestDecision` · `giftCardIssue` ·
`giftCardActivate` · `giftCardClaim` · `goldPriceUpdate` · `hijabTryOn` ·
`officeVoiceParse` · `onlineOrderPlace` · `onlineOrderRelease` ·
`onlineOrderExpire` · `paymobTerminalOrder` · `paymobWebhook` · `rateVisitPush`
· `staffAccessSet`
**europe-west1 (10):** `onRewardAdded` · `onWelcomeToken` · `onCatalogUpdated` ·
`onSaleForReferral` · `onLeaveRequest` · `onOfficePushTest` · `onShortageReported`
· `onStaffOrder` · `onStaffRegistration` · `autoCloseSalesShiftsAt1`
⚠️ `functions/index.js` مش في الريبو — عند المالك في `echarpe-push`.
⚠️ **Node.js 20 بينتهي 30 أكتوبر 2026** — لازم ترقية قبلها.

## 17. 🔐 قواعد الأمان
فاز 2 منشورة. إصلاح تسجيل العميلة: الحقول الزيادة مسموحة بشرط points=0 عند الإنشاء.
**فاز 3 معلّق:** نقاط الشراء لسه client-side في pos-sale.js.

## 18. ⚠️ القاعدة الذهبية (الباج المتكرر)
`sales/index.html` أكتر من `<script>` — `const/let` مش بتعدّي البلوكات.
الحل: `window.x = x` بعد التعريف. **يتفحص مع كل ميزة.** (ضربت تاني مع swapHoursFrom.)

## 19. الاختبارات (367 · 9 ملفات)
```
node tests/run.js
```
`test-action-bar` · `test-admin-panels` · `test-frames` · `test-pos-syntax`
· `test-sales-boot` · `test-sales-roles` · `test-time-credit`
· **`test-payment-totals`** (فكة/عكس/تقارير/تقفيل/يوم شغل/أوفلاين/نطاق/ثغرة النقط)
· **`test-payroll-money`** (computeSalary فعليًا: رصيد وقت/إدارية/يوم 31/كسور/UTC)
- منهجية مثبتة: **اختبار سلبي لكل إصلاح** (رجّع الباج → الاختبار لازم يقع).

## 20. ✅ اتفحص وسليم (مسح الثغرات الشامل)
سقف شراء الموظفة (المعلّق محسوب) · حارس كمية المرتجع عبر الجلسات · سقف الخصم
بالدور + تسجيل · مكافأة العميل مسقوفة بالفاتورة · تعقيم أرقام التطبيق ·
منع تكرار الحفظ · تهريب HTML في الشات · عهدة التحويلات 30 دقيقة · نافذة/سقف السلف.
- ملحوظة مقصودة: البيع بكمية أكبر من المخزون مسموح (تعليق صريح) — المخزون
  ممكن ينزل سالب من غير تنبيه. المالك ماطلبش تغيير.

## 20أ. 🧕 التجربة (TryOn) — تعديلات 19 سبتمبر
- **شكل اللفة** بقى موصوف صراحة في `DRAPE_STYLE` في أول `hijabTryOn.js`:
  لفة واحدة حوالين الرقبة + طرف طويل سايب قدام + قماش مرتخي + من غير شد تحت
  الدقن ولا دبابيس. قبلها البرومبت ماكانش بيوصف اللفة خالص فالموديل كان بيختار
  الشكل التقليدي. **التعديل كلام مش كود — مكان واحد بس.**
- **قص الشبكة اتصلح** في `tryon/photo-core.js`. الحماية القديمة
  (`resolveGridOrientation`) كانت بتخرج فورًا لو الشبكة مربعة، و2×2 مربعة.
  والموديل مش ملزوم يطيع: بيرجّع أحيانًا شريط 1×4 بدل 2×2، فالقص كان بيقطع
  نص وش من خانة ونص من اللي جنبها. دلوقتي `detectGridLayout` بيكتشف الشكل
  الحقيقي من نسبة الصورة.
- ✅ **مؤكَّد:** 3 ألوان بندانة + خانة «بدون» = 4 خانات = **توليد واحد** 2×2
  بدقة 2K. اللون بيتغيّر بالكود على الجهاز مجانًا.

## 20ب. 🌐 فكرة موقع «العميل هو الموديل» (مطروحة، مش مبدوءة)
موقع منفصل: العميلة ترفع صورتها مرة، وتتصفّح الكتالوج وكل منتج عليها.
**التكلفة هي اللي هتقرر تعيش ولا لأ.** خطة التقليل بالترتيب:
1. متولّدش غير اللي بتشوفه (عند فتح المنتج، مش الكتالوج كله)
2. توليد واحد = 4 منتجات (نفس حيلة 2×2 بس على المنتجات)
3. كاش دائم لكل (صورة العميلة × المنتج) في **Storage** مش Firestore
4. اللون بالكود مش بالتوليد (معمول أصلًا)
5. سقف يومي لكل عميلة

## 21. المعلّق
### 🔥 الأولوية (متفق عليها مع المالك 19 سبتمبر — بالترتيب ده)
0. 🔴 **العيب المحاسبي (4هـ)** — الخصومات كسطور سالبة بتبوّظ رقم المبيعات.
   محتاج قرار المالك في النقط، وجلسة لوحده.
1. ~~المرتجع بأربع طرق~~ ✅ اتعمل واتجرّب (4د).
2. **سجل الرصيد الكامل** — السبب · رقم الفاتورة الأصلية · طريقة الدفع اللي
   كانت · مين عملها · كشف حركات. + **منع السحب مرتين** بمعاملة ذرّية على
   السيرفر (مش في الواجهة — جهازين على نفس الحساب بيعدّوا من أي فحص متصفح).
3. **PIN العميلة** يتسجّل من تابلت الفرع بتصميم وأنيميشن، ويبقى مطلوب للصرف
   من الرصيد في الفرع وأونلاين. العميلات اللي عندهم رصيد قديم: أول صرف
   يطلب منهم PIN جديد.
4. **سيناريو النت المفصول** مع المرتجع والنقط — تحقق واختبارات.
5. **زرار «اضف رصيد»** — مش موجود في POS ولا Office. Office فيه طابور
   موافقات بس **مفيش أي كود في POS بيكتب في `credit_requests`** — ميزة
   ناقصة نصها. الطريق الوحيد الشغال دلوقتي هو الباقي.

### باقي المعلّق
6. **مراقبة أول تقفيلين يوم بعد التحديث** — بانر الفواتير المتأخرة هو الدليل الحي.
7. **الدرج في فرع echarpe** — بناء exe بآخر main.js وقراءة رسالة الخطأ.
8. **استيراد ملف echarpe الفعلي** (مسح + خصم كميات) ثم **جرد فعلي**.
9. **Paymob للفروع التانية** + حسم "You haven't set up a POS".
10. **قرارين مستنيين المالك:** تحصين office (أ/ب/ج) · زرار خصم الغياب (إضافة ولا بديل).
11. **فاز 3 أمنيًا:** نقاط الشراء server-side.
12. **فصل مخزون البراندين** لو Glow هتشتغل جنب echarpe.
13. **ترقية Node.js 20 قبل 30 أكتوبر 2026** · تحديث تلقائي للـ exe (electron-updater) · مراقبة أخطاء · أتمتة CACHE_NAME.
14. تبويب "نشاط غريب" (POS + office) بعد فترة الاستقرار على بيانات حقيقية.
15. تنضيف `tests/run.js` من فشل أرقام إصدارات ChatGPT · إعادة بناء اختبارات النسخة المحلية الضايعة (كان فيها test-no-prompt,
    test-drawer, test-paymob... إلخ) لو المالك لقاها على جهازه يرفعها.
