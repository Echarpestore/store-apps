# AI_HANDOFF.md — Echarpe / Glow Retail System

> المرجع الوحيد للحالة. يتحدّث مع كل ميزة/إصلاح/تغيير في قاعدة البيانات.
> المالك: **محمود** (سلسلة ريتيل: طرح، بيچامات، ملابس منزلية). عربي مصري.
> كود كامل شغّال خطوة خطوة، أقل أسئلة، **متختبر بالـ harness قبل التسليم**.

---

## 0. قواعد التعامل (مهمة — اتفقنا عليها)
- **الملفات تتسلّم بأسمائها الحقيقية دايمًا** — ومعاها **الفولدر**.
  لو فيه ملفين بنفس الاسم (`sw.js` في pos وsales وOffice · `app.js` في pos) →
  **يتفصلوا صراحةً**. حصلت غلطة: نسخة قديمة من `pos/app.js` كانت متحطة في
  `sales/app.js` واتمسحت في 31/7.
- **الملف يتسلّم كامل**، مش أجزاء يلصقها بنفسه.
- الاختبار بالـ harness **قبل** التسليم + **اختبار سلبي**: رجّع الباج واتأكد إن الاختبار بيقع.
- الرفع: **دفعة واحدة ← علامة خضراء ← الدفعة اللي بعدها**. وبعد الرفع افتح/اقفل مرتين.
- **كل تعديل يرفع `CACHE_NAME` في `sw.js`** بتاع التطبيق المعني.
- ⚠️ قبل تسليم أي ملف، اتأكد إنه أحدث نسخة (grep على ميزة معروفة فيه).
- **GitHub هو المرجع** (قرار المالك) — مش النسخ المحلية.

### ⚠️⚠️ فخ الاختبارات الفضفاضة (تكرر 4 مرات في جلسة 31/7)
اختبار بيدوّر على **اسم دالة** أو **نص** في المصدر بيعدّي حتى لو الباج رجع،
لأن الاسم موجود في مكان تاني (تعريف · فرع مختلف · تعليق · رسالة). أمثلة وقعت:
- `if(!ok) return;` — موجودة في فرع السحب الزايد كمان
- `findCardLeg(...)` — موجودة في فرع الرفض بـ`const`
- `d.status === 'refunded'` — موجودة في نص الرسالة
- `window.normalizeScan` — موجودة في سطر التعريف مش النداء
- منع الباركود المكرر — الاختبار كان بيفحص وجود النص بس، فتعطيل المنع عدّى

**القاعدة:** التأكيد لازم يمسك **الشكل الكامل للسطر** (`let _leg = findCardLeg(...)`)
أو **الشرط نفسه**، ويُفضّل **اختبار سلوكي** بدل فحص النص. وكل إصلاح لازم
يتعمله اختبار سلبي **فعلي** — الاختبار السلبي هو اللي مسك الأربعة دول.

### ⚠️ استخراج الدوال
regex بينتهي عند سطر معيّن بيتكسر مع أي refactor (حصل مع
`renderViolationsReview();` لما بقت `_safe(()=> renderViolationsReview(), ...)`
وطلّع 13 فشل وهمي). استخدم **استخراج بالأقواس المتوازنة** (`extractFn`).

---

## 1. الإصدارات الحالية (31 يوليو 2026)
| التطبيق | الإصدار |
|---|---|
| POS | **v249** |
| sales (الحضور/HR) | **v74** |
| echarpe office | **v7** |
| feedback | v18 |
| loyalty / glow | v36 / v29 |
| Cloud Functions | **6 دوال** (اتشال منهم دالتين المرتجع) |
| الاختبارات | **899 ناجح · 0 فاشل · 13 ملف** (`node tests/run.js`) |

> جلسة 31/7 بدأت على **537 ناجح · 30 فاشل** رغم إن المستند كان بيقول 713 · 0.

---

## 2. المعمار
- Vanilla JS، مفيش build. Firebase compat (POS/apps) + modular (sales).
- Firestore: `customer-feedback-8ac1d` · offline persistence شغّالة.
- الاستضافة: GitHub Pages — رفع يدوي. الدومين: `echarpe.store/pos`.
- ⚠️ **GitHub Pages بيقدّم كل ملفات JS للعامة** — أي سر في الكود مقروء لأي حد.
- **Cloud Functions** من `C:\Users\Mahmo\echarpe-push`:
  `cd echarpe-push && firebase deploy --only functions`
  ⚠️ الويب هوك في `us-central1` وباقي الدوال في `europe-west1`.
- **Electron POS** من `C:\Users\Mahmo\Documents\pos-desktop` → `npm run build`.
- **قرار المالك: تطبيق sales عمره ما هيشتغل ويندوز/Electron.**

---

## 3. الملفات
- `pos/` — `index.html` · `pos-core.js` · `pos-sale.js` · `pos-admin.js` · `pos-reports.js`
  · `app.js` · `frames.js` · `import.js` · `products.js` · `transfers.js` · `chat.js`
  · `staff.js` · `profiles.js` · `loyalty.js` · `search.js` · `discounts.js` · `ui-editor.js` + `sw.js`
- `sales/` — `index.html` + `sales-app.js` + `sales-ui.js` + `sw.js`
  (⚠️ `sales/app.js` كان ملف يتيم — نسخة قديمة من `pos/app.js` — **اتمسح 31/7**)
- `Office/` — `index.html` · `office.js` · `sw.js`
- `tests/` — `run.js` + `helpers/` + **13 ملف اختبار**
- `docs/` — `DEVELOPER.md` · `OPERATIONS.md`

---

## 4. 🔴 جلسة 31 يوليو — الأهم

### 4أ. تعليق الكاشير مع الفيزا (السبب الجذري)
**إعادة هيكلة الكارتين غيّرت التأكيد من «حط علامتين عالميتين» لـ«لاقي الشريحة
بالرقم وعدّل عليها»** — والبحث كان `if(_leg)` **من غير else**. لو مالقاش،
التأكيد يوصل والفلوس تتسحب والكود يعدّي بصمت. النتيجة **عرضين من إصابة واحدة**:
الشريحة تفضل `pending` (زرار الحفظ مقفول للأبد) و`cardTxns` تتبني من
`approved` بس (فاتورة من غير بيانات كارت).

**اتصلح:**
1. `findCardLeg(legs, seq, ref)` — لقط بـ3 طبقات: الرقم ← مرجع الطلب ← الشريحة
   المعلّقة الوحيدة. ومبيخمّنش لو فيه أكتر من واحدة.
2. تأكيد مالقاش شريحة → **بينشئ واحدة** ويسجل `card_leg_recovered`.
3. **الحفظ اليدوي**: الشريحة المعلّقة مابقتش تقفل الزرار. الحماية اتنقلت لشاشة
   تأكيد إجبارية («إيصال الموافقة طلع من الماكينة؟»). الشريحة بتتسجل حالة
   `manual` وبتتكتب في `cardTxns` بعلامة **`manual: true`** للمراجعة مع Paymob.
   وبيتسجل `card_saved_manual`.
   - **قبل كده:** رسالة الـ10 دقايق كانت بتقول «احفظ يدوي» والزرار مقفول = طريق مسدود.
4. الحالات `refunded` و`pending` بقت متعالجة (كانت المتابعة تستنى للأبد).

### 4ب. الويب هوك — `success` كانت آخر واحدة في السلّم
`if(refunded) → else if(voided) → else if(pending) → else if(success)`.
1. **`pending` قبل `success`:** عملية success:true و pending:true (حجز البنك)
   كانت بتتكتب "pending" والـPOS مبيتحركش عليها.
2. **`is_refunded` بتفضل true على البيعة الأصلية** بعد أي مرتجع → أي callback
   متأخر كان بيدوس على "success".

**اتصلح:** الحالة بتتحسب **بعد `isChild`** لأن معناها بيختلف:
- **مستند البيعة:** `success → pending → voided → refunded`
- **مستند المرتجع (child):** `voided → refunded → success → pending`
- **حارس:** بيعة نجحت عمرها ما تتنزّل — بيتكتب `lateStatus` بدل ما تتلمس.
- ⚡ **مسار السرعة:** التأكيد الناجح كتابة واحدة **بصفر قراءة**؛ الحارس
  (transaction) على الحالات النازلة النادرة بس.

**اتأكد على بيانات حقيقية:** عملية 165 ج.م (30/7، الرحاب، txn 505607387)
`approvalCode:"000"` و`declineReason:"Accepted"` واتسجلت **`refunded`**.
بعد النشر: 4 عمليات كلهم `success` صح.

**⏱️ قياس التأخير (4 عمليات، فرعين، 3 شبكات كروت):**
موافقة البنك ← كتابة Firestore = **2.7 أو 3.5 ثانية** — **قيمتين بس، مفيش تالت**.
الاستنتاج: **دورة إرسال ثابتة عند Paymob**، مش بدء بارد ولا شبكة.
→ `minInstances` **مش هيفرق** · نقل الدالة لأوروبا يوفّر ~0.3 ث بس.
→ الحاجة الوحيدة اللي بتشيلها فعلًا هي **ECR/USB** (الماكينة توصل الجهاز مباشرة).

### 4ج. 🪟 «السيستم بيقف عن الكتابة» — أصعب مشكلة في الجلسة
**العرض:** كل الشاشات، مفيش كتابة خالص، **والحل عند الفروع إنهم يفتحوا
QuickBooks ويكتبوا فيه ويرجعوا**.

**التشخيص:** ده **مش** ضياع تركيز جوه الصفحة — الرجوع من برنامج تاني كان
هيرجّع نفس الحالة. ده **تركيز نظام التشغيل**.

**السبب:** البرنامج بيفتح **8 نوافذ منبثقة** للطباعة. `pos-reports.js` كان
بياخد التركيز بـ`w.focus()` ويطبع و**`w.close()` مش موجود خالص** — النافذة
تفضل مفتوحة ماسكة التركيز. وولا واحدة من الـ8 كانت بترجّع التركيز للنافذة
الرئيسية بعد ما تقفل.

**اتصلح:** `reclaimWindowFocus()` في `pos-core.js` — بترجّع تركيز النافذة
والخانة اللي الكاشير كانت فيها، **بمحاولتين** (400 مللي و1.8 ث). وكل الـ8
مواضع بقت تناديها. والنافذة اللي مكانتش بتقفل بقت تقفل.

⚠️ **الإثبات الوحيد إنها تبطّل تحصل — مقدرش أختبرها من غير ويندوز.**
**لو فضلت:** الشك التالي على أمر الدرج في Electron (PowerShell بيفتح نافذة
كونسول بتخطف التركيز) — محتاج `main.js` من `pos-desktop`.

**وفيه طبقة تانية أخف:** `focusSearchBar()` بتتنادى وقت الدخول للشاشة بس، فأي
نافذة بتتقفل بتاخد التركيز معاها. اتحط **حارس تركيز** في `app.js` بيشتغل في
شاشة البيع بس، لما مفيش نافذة مفتوحة، ولما **مفيش أي حاجة متفوكسة خالص**.
- 🔑 وبيبان لاج ليه: لما مفيش خانة متفوكسة، معالج المسح العام بيبلع الحروف في
  `_gsBuf` — الكاشير بتكتب ومفيش حاجة بتظهر، وEnter بيتفسّر كمسح كود.

### 4د. 💰 الدرج مكانش بيفتح في المرتجع
`hasCash = Number(payments.cash) > 0` — وفاتورة العكس بتحمل مدفوعات **سالبة**.
بقى `Math.abs(...) > 0.005`. **مش مشكلة هاردوير** — كانت في كل الفروع حتى Glow.

### 4هـ. ⌨️ كارت الموظف واللغة عربي
خريطة الكيبورد فيها **52 مدخل كلهم حروف — ولا رقم**. وكارت الموظف
`EC[A-Z2-9]{10}` فيه أرقام → الحروف تتصلّح والأرقام تفضل عربية → الشكل ميطابقش.
- اتضافت الأرقام الهندية (٠-٩) والفارسية (۰-۹).
- `trScanInput` و`trCarrierInput` في التحويلات كانوا بياخدوا القيمة **خام** —
  بقوا يعدّوا على `normalizeScan`.
- ⚠️ اتأكد إن البحث بالاسم العربي ماتخربش («قميص قطن» · «طرحة ٣٤»).

### 4و. ⚡ لاج الكتابة في شاشة البيع (منفصل عن 4ج)
كل ضغطة زرار كانت بتعيد تطبيع **اسم كل صنف في المخزون** — 7 regex × عدد الأصناف.
اتحطت ذاكرة على الدالة نفسها (`searchNorm._c`) — **×3 لـ×5 أسرع**.
- ⚠️ الذاكرة على **الدالة** مش متغير برّاني — الاختبارات بتستخرج الدوال فرادى
  وأي حالة برّانية بتوقّع الهارنس.
- ⚠️ **المخزون أقل من 2000 صنف** فالمكسب الحقيقي محدود — السبب الأكبر كان 4ج.

### 4ز. 🔍 كود ضايع من GitHub (رفعة قديمة داست على جديدة)
- `barcodePrefix` **مش موجودة خالص** في `pos-core.js` — الـ4 مواضع كانت بتقع
  على `startsWith` الاحتياطي بصمت.
- `search.js` لسه على `.includes` — «33 بتطلّع 533» كان **حي**.
- `pos-admin.js` نسخة أقدم: بحث المخزون `.includes` · مفيش `toggleInvAddForm`
  ولا `openDupBarcodeCheck` · **ومفيش أي منع للباركود المكرر**.

### 4ح. 📦 المخزون
- **منع الباركود المكرر** — `barcodeDupReject(list, barcode, selfId)` **دالة نقية**.
  كان `.add(data)` على طول. صنفين بنفس الكود = المسدس بيقف قدام تطابقين.
- زرار **🔍 أكواد متكررة** — المنع بيحمي الجديد بس، والقديم لسه موجود.
- فورم الإضافة ورا زرار (كان مفرود دايمًا)، وتحذير الحذف لو الصنف عليه كمية.
- ترتيب الاقتراحات **قبل** `slice(0,10)` — كان بيقص وبعدين يرتب، فالمطابقة
  التامة نفسها ممكن تتقص. (نفس الباج اتصلح في `search.js` و`pos-sale.js`.)

---

## 5. 🔐 أمان Office (اتصلح 31/7)
**كان:**
- `const OWNER_CODE = '2005'` **مكتوب صريح** — وGitHub Pages بيقدّم الملف لأي
  حد. مش محتاج جهازك ولا devtools.
- الباسورد في `localStorage` بـXOR + base64 — **تشويش مش تشفير**.
- `office_owner_ok='1'` — علامة قابلة للزرع من devtools، ومفيش انتهاء صلاحية.

**بقى:**
- الكود **بصمة SHA-256 بملح** في `pos_test_settings/office_gate`. الكود نفسه
  عمره ما بيتخزّن ولا بيتبعت.
- الباسورد مابقاش يتخزّن — `setPersistence(LOCAL)` والتوكن **ينفع يتلغي**.
- جلسة **10 ساعات** في `sessionStorage`. **تغيير الكود بيبطّل كل الجلسات** على
  كل الأجهزة تلقائي (البصمة جوه الجلسة بتتقارن).
- تهدئة بعد 5 محاولات + `officeChangeCode()`.
- **أول تشغيل** بيطلب كود جديد — آمن لأنه بعد دخول Firebase.
- 🆘 **الاسترجاع:** امسح `pos_test_settings/office_gate` من الكونسول.
- ⚠️ **بصراحة:** أي حاجة على المتصفح قابلة للزرع لو الجهاز مفتوح ومعاه devtools.
  الحماية الحقيقية = Firebase Auth + قواعد Firestore. البوابة لسرقة الجهاز.

---

## 6. 📅 Office — شاشة اليوم (اتبنت 31/7)
تبويب **📅 اليوم** أول واحد. اختيار الفرع + التاريخ + (امبارح/النهاردة/بكرة)،
وتحته 3 تبويبات: **💵 الدفع** · **🧾 المبيعات** · **📦 الأصناف**.

**اللي خلاها تطابق التقفيل:**
- `ofBizDayRange()` — يوم الشغل من `day_cfg.startHour` (افتراضي 6) بتوقيت
  القاهرة، ومظبوطة صيفًا وشتاءً.
- `ofSaleTs()` — نسخة من `saleTs` بحارس الـ48 ساعة (فواتير الأوفلاين).
- **الدفع بمنطق التقفيل** (المعكوسة والعكس مع بعض) · **الأصناف بمنطق التقارير**
  (الطرفين مستبعدين). **الفرق مكتوب على الشاشة** عشان الرقمين ممكن يفرقوا.
- استعلام بنطاق زمني **على السيرفر** + فلترة تانية بالوقت الحقيقي.
- ⚠️ ممكن يطلب إنشاء index أول مرة.

---

## 7. 💳 Paymob
`paymobTerminalOrder` · `paymobWebhook` (HMAC-SHA512 على 20 حقل + timingSafeEqual).
- **اتشال 31/7:** `paymobRefundProbe` · `paymobRefundTerminalProbe` + سرّينهم
  (`PAYMOB_REFUND_TOKEN` · `PAYMOB_SECRET_KEY`). المرتجع مؤجل.
- ⚠️ **`PAYMOB_API_KEY` باقي** — `paymobTerminalOrder` بيستخدمه.
- **`pos_paymob_rejected`** — تسجيل رفض الـHMAC كان **مش منشور** رغم إن
  المستند كان بيقول تتراقب. اتنشر 31/7. **مجموعة فاضية دلوقتي = معلومة حقيقية.**
- Webhook URL في Paymob (**متغيرش**):
  `https://us-central1-customer-feedback-8ac1d.cloudfunctions.net/paymobWebhook`
- Integration: 5228563 (echarpe) · 5563036 (Glow). ترمينال: 905225 · 985507.
- ⚠️ "You haven't set up a POS" تحت MID 629614 — لسه مع Paymob.

---

## 8. 🖨️ الفاتورة والدرج
- الطباعة **آخر خطوة** في طابور: رقم الفاتورة ← حفظ البيعة ← المرتجع ← أوردر
  الموظفة ← خصم المخزون ← النقط ← بيانات العميلة ← ربط الفيدباك ← **الطباعة**.
  كل خطوة `await` على الشبكة. **الترتيب ده مقصود** — ورقة لبيعة مش متسجلة أوحش.
- **`print_latency`** اتحط في `pos-sale.js` بيسجل في سجل النشاط:
  `paymobMs` (بتاعهم) · `deliverMs` (Firestore←الجهاز) · `saveMs` (بتاعنا) · `totalMs`.
  **[PENDING] استنى الأرقام قبل أي تعديل على ترتيب الحفظ.**
- ⚠️ **الطباعة من السيرفر مش هتسرّع** — السيرفر مايوصلش للطابعة (شبكة محلية)،
  ومايعرفش السلة، **وبتكسر الأوفلاين**. تنفع كمسار إنقاذ بس.
- **💰 الدرج معلّق في فرع echarpe:** `rawToPrinter` خام عبر winspool. Star TSP100
  مش بتفهم ESC/POS — أرجح سبب إن Glow شغال وecharpe لأ. **[PENDING]**

---

## 9. 🕕 يوم الشغل
`bizDayStartMs/bizDayKey/isSameBizDay` في pos-core · الساعة الفاصلة من
`pos_test_settings/day_cfg.startHour` (افتراضي 6) · مربوطة في: التقفيل والتقارير
والمرتجع وترند الأيام **وشاشة اليوم في Office**. الفواتير القديمة بتاريخها التقويمي.

---

## 10. 🔍 المسح والباركود
- `barcodePrefix(bc, q)` — **بداية** مش احتواء. مستخدمة في 5 مواضع بحزام
  احتياطي `_bp`. ⚠️ الحزام لحظة تحميل ملفات مختلطة، **مش بديل دايم**.
- `searchNorm` بذاكرة على الدالة · `searchMatch` بتقطيع مرة لكل استعلام.
- `normalizeScan` — بتترجم بس لو الناتج شكل كود معروف (FT/ECH/GLW/كارت موظف).

---

## 11. ⚠️ ممنوع `prompt()` في POS بس — Electron مش بيدعمها
البديل `askText()`. sales وOffice معفيين (قرار المالك).

---

## 12. 🔐 أدوار sales
مالك 2005 · مدير من `pos_test_settings/sales_roles.managerCode`.
`roleCan` فيه حارس `adminUnlocked` — **جلسة مقفولة = صفر صلاحيات**، والحارس
**قبل** فحص الدور. عليه اختبار سلوكي صريح دلوقتي.
«إعدادات السلف» perm=`settings` (لوحة إعدادات مش عرض فلوس) — ومحجوبة عن المدير.

---

## 13. المجموعات
`pos_test_sales`: + `cashReceived` · `changeGiven` · `createdAtMs` · فاتورة العكس
فيها `payments` سالبة · `cardTxns[]` فيها **`manual:true`** للمسجّل يدوي.
`pos_paymob_txns`: + `lateStatus`/`lateStatusTxn`/`lateStatusAt` · `isChild` · `parentOrderRef`.
`pos_paymob_rejected`: `reason:'bad_hmac'` · `merchantOrderId` · `txnId` · `success` · `pending`.
`pos_test_settings/office_gate`: `{ hash, setAt }`.
سجل النشاط: `card_leg_recovered` · `card_saved_manual` · `print_latency`.

---

## 14. الدوال المنشورة (6)
`onRewardAdded` · `onWelcomeToken` · `onCatalogUpdated` · `onSaleForReferral`
· `paymobTerminalOrder` · `paymobWebhook`
(`functions/index.js` مش في الريبو — عند المالك في `echarpe-push`.
معاه `verify-status.js` لفحص سلّم الحالة: `node verify-status.js` → 8 · 0.)

---

## 15. ⚠️ القاعدة الذهبية
`sales/index.html` أكتر من `<script>` — `const/let` مش بتعدّي البلوكات.
الحل: `window.x = x` بعد التعريف. **يتفحص مع كل ميزة.**

---

## 16. الاختبارات (899 · 13 ملف)
```
node tests/run.js
```
`test-action-bar` · `test-admin-panels` · `test-frames` · `test-labels`
· `test-multi-card` · `test-payment-totals` · `test-paymob-refund`
· `test-payroll-money` · `test-pos-syntax` · `test-sales-boot` · `test-sales-roles`
· `test-time-credit` · **`test-office-auth`** (أمان Office + شاشة اليوم)

---

## 17. المعلّق — بالأولوية

### 🔴 فلوس حقيقية معلقة
1. **فلتر `pos_paymob_txns`** على `status == "refunded"` و`"pending"`. أي مستند
   فيه `approvalCode:"000"` و**مفيهوش `isChild`** = بيعة ناجحة اتسجلت غلط
   ومحتاجة تصحيح في تقفيل يومها. عملية الـ165 (30/7) متأكدة.
   ⚠️ ولازم تتأكد من لوحة Paymob **ليه** اتعملت refunded (مرتجع فعلي ولا عكس تلقائي).

### 🟠 تأكيد إن إصلاحات 31/7 اشتغلت
2. **مشكلة الكتابة بطّلت؟** لو لأ → محتاج `main.js` من `pos-desktop`.
3. كارت الموظفة بالعربي اشتغل؟
4. `card_leg_recovered` ظهر؟ (= التأكيد كان بيضيع فعلًا)
5. `pos_paymob_rejected` لازم تفضل فاضية.
6. `print_latency` — بعد كام فاتورة يبان الجزء الناقص من زمن الطباعة.
7. قارن ملخص الدفع في شاشة اليوم بتقفيل الفرع الفعلي.

### 🟡 Office — الفاضل (بترتيب المالك)
8. **التاسك الأسبوعي** — ⚠️ `sales_tasks` معرّف المستند فيه = رقم الموظفة،
   فأي تاسك جديد **بيمسح القديم**. مفيش أسبوع ولا حالة ولا تاريخ.
   محتاج مستند لكل أسبوع مع الحفاظ على القراءة الحالية في تطبيق الحضور.
9. **حساب التجار** — كشف حساب برصيد جاري + تعديل ومسح الدفعات.
10. **المصاريف المتكررة** — الإيجار/النت يتولّدوا مستحقة أول الشهر.

### ⚪ الباقي
11. الدرج في فرع echarpe · استيراد ملف echarpe الفعلي ثم جرد.
12. Paymob لباقي الفروع + حسم "You haven't set up a POS".
13. **ECR/USB** — الحاجة الوحيدة اللي بتشيل الـ3.1 ثانية. اسأل Paymob:
    هل الترمينال بيدعم integrated mode؟ ⚠️ **مش قبل ما `print_latency` يبان.**
14. فاز 3 أمنيًا: نقاط الشراء server-side.
15. **فصل مخزون البراندين** — الباركود = مفتاح المستند في الاستيراد.
16. زرار خصم الغياب: إضافة ولا بديل؟ (**قرار مستني المالك**)
17. `verBadge` **مش موجود في أي ملف** — `device_versions` بتتسجل بنسخة فاضية
    لكل جهاز. لوحة «مين اتحدّث» ميتة.

---

## 18. ✅ اتفحص وسليم
سقف شراء الموظفة · حارس كمية المرتجع عبر الجلسات · سقف الخصم بالدور · مكافأة
العميل مسقوفة بالفاتورة · تعقيم أرقام التطبيق · منع تكرار الحفظ · تهريب HTML في
الشات · عهدة التحويلات 30 دقيقة · نافذة/سقف السلف · ثغرة النقط→كاش (4 طبقات).
- ملحوظة مقصودة: البيع بكمية أكبر من المخزون مسموح — المخزون ممكن ينزل سالب.
