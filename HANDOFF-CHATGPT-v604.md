# HANDOFF لـChatGPT — منع تكرار نفس الباجات
**مشروع echarpe / Glow · آخر تحديث: v604**

> الملف ده مش نصايح عامة. ده قايمة بـ**باجات اترجعت أكتر من مرة**،
> وكل واحدة فيهم كلّفت جلسة كاملة عشان تتلاقى تاني.
> اقرا القسم 1 و2 قبل أي تسليم — دول اللي بيتكرروا.

---

## 🚨 1. ملفات ممنوع تبعتها إلا لو التعديل جواها هي نفسها

### `tests/run.js` — **اترجع 3 مرات**
الملف ده فيه حارس ضروري:
```js
const realExit = process.exit.bind(process);
process.exit = function(code){ const e=new Error('exit'); e.__exitStub=true; e.__exitCode=code||0; throw e; };
// ... الحلقة ...
process.exit = realExit;
```

**من غيره:** أي ملف اختبار فيه `process.exit()` (زي
`test-paymob-retry-integrity-v435.js`) بيقفل عملية node **فورًا**،
فالهارنس بيقف عند الملف **109 من 192** والباقي **مبيشتغلش خالص من غير ولا رسالة**.

الشاشة بتبان طبيعية تمامًا. محدش بياخد باله. الباجات بتعدي.

**القاعدة:** لو مش بتعدّل في `run.js` نفسه — **متحطهوش في الـzip**.
ولو حطيته، تأكد إن الحارس جواه.

**فحص سريع قبل أي تسليم:**
```bash
node tests/run.js | grep -c "^▶"     # لازم = عدد ملفات tests/test-*.js
ls tests/test-*.js | wc -l
```
الرقمين لازم يبقوا متساويين. لو مختلفين، فيه ملفات بتموت بصمت.

---

## 🔁 2. باج التاريخ في sales — اترجع 3 مرات كمان

### المعمار (لازم تفهمه قبل ما تلمسه)
`sales-app.js` بيقرا التاريخ بنظام اسمه **LF431** لتقليل قراءات Firestore:

| الطبقة | المصدر | المدة |
|---|---|---|
| كاش فوري | IndexedDB محلي | كل اللي متخزن |
| تحديث كامل | السيرفر، **مرة كل 24 ساعة** | 190 يوم |
| مستمع حي | `onSnapshot` | آخر **يومين** بس |

علامة "التاريخ طازة" بتتخزن في **`localStorage`** بالبادئة `sales_lf_v431_`.

### ⚠️ نقطة الانهيار
علامات `localStorage` **بتعيش عبر التحديثات**، لكن صفوف التاريخ في
**IndexedDB** ممكن تضيع أو الـpersistence تفشل تشتغل بعد التحديث.

ساعتها العلامة بتقول *"مفيش داعي تسأل السيرفر"* والكاش فاضي →
التطبيق **ميجيبش الـ190 يوم خالص** ويفضل بس مستمع اليومين →
**التاريخ يبان بادئ من يوم التحديث.**

### ❌ الغلط اللي حصل في v603
ربطت الإصلاح بعلم **دائم**:
```js
if(localStorage.getItem('sales_history_repair_v603') !== '1'){
    // امسح علامات LF431
    localStorage.setItem('sales_history_repair_v603','1');   // ← للأبد
}
```
اشتغل **مرة واحدة**. أول تحديث اتصلح، **وكل تحديث بعده لأ**.
عشان كده المالك قال: *"بتحصل دايمًا بعد تحديث ChatGPT"* — وكان محق.

### ✅ الصح (v604)
```js
const SALES_BUILD='604';
if(localStorage.getItem('sales_history_repair_build') !== SALES_BUILD){
    Object.keys(localStorage).forEach(k=>{ if(k.indexOf(LF431_PREFIX)===0) localStorage.removeItem(k); });
    localStorage.setItem('sales_history_repair_build', SALES_BUILD);
}
```

### 📌 قاعدة إجبارية من دلوقتي
**التلاتة دول يترفعوا مع بعض في نفس التعديل:**

| الحاجة | المكان |
|---|---|
| `SALES_BUILD` | `sales/sales-app.js` |
| `CACHE_NAME` | `sales/sw.js` |
| `sales-app.js?v=` | `sales/index.html` |

لو رفعت اتنين ونسيت التالت → الباج يرجع.

### النص التاني من نفس المشكلة (اتصلح في v604)
الاستعلام كان مربوط كده:
```js
_salesInitialAuthReady.then(() => getDocsFromServer(fullQ))   // ❌
```
`_salesInitialAuthReady` **بيرجع `false`** لو إعادة الدخول وقت الفتح فشلت،
والكود كان **بيتجاهل القيمة** ويطلق الاستعلام وهو مش مسجّل دخول.

وقواعد Firestore بتشترط دخول **بإيميل وباسورد**:
```
match /sales_points/{id} { allow read, write: if isStaff(); }
```
→ `permission-denied` → المحاولة الوحيدة بعد 5 ثواني برضه فاشلة →
**مفيش أي محاولة تانية طول الجلسة**.

**الصح:** الاستعلام يستنى الدخول يتأكد (`_lf431Authed()`)، ويتسجل في طابور
(`_lf431Pending`) لو لسه، ويتنفّذ أول ما `onAuthStateChanged` يقول connected.
**متشلش الحراس دي.**

---

## 🧪 3. أخطاء متكررة في كتابة الاختبارات

### 3.1 متثبّتش رقم نسخة
```js
assert(sw.includes('echarpe-office-v513'));           // ❌ هيكسر كل جلسة
assert(Number((sw.match(/echarpe-office-v(\d+)/)||[])[1]||0) >= 513);  // ✅
```
إحنا **مفروض نرفع `CACHE_NAME` كل جلسة**. الاختبار المثبّت مصمّم إنه يكسر.
اتصلح ده في 5 ملفات لحد دلوقتي.

### 3.2 الاختبار لازم يفحص **النية** مش **التنفيذ**
حصل مرتين وكل مرة كان هيجبرنا نرجّع باج:

**مثال 1 —** اختبار كان بيقول `assert(agent.includes('-c:v copy'))`.
بس نسخ HEVC جوه MP4 بيرجع HTTP 200 و**أندرويد وكروم مش بيعرفوا يفكّوه**
(الفيديو أسود). الحل الصح كان `h264_qsv`. الاختبار كان هيمنع الإصلاح.

**مثال 2 —** `assert(s.includes("sales_history_repair_v603"))` —
ده كان بيثبّت **العلم الدائم اللي هو نفسه الباج**.

**القاعدة:** افحص إن فيه إصلاح بيعمل الوظيفة، مش إن السطر الفلاني موجود بالحرف.

### 3.3 اختبار سلبي إجباري
بعد أي إصلاح: **رجّع الباج واتأكد إن الاختبار وقع فعلًا.**
لو عدّى وهو راجع، الاختبار مش بيفحص حاجة.

فخ متكرر: الاختبار بيدوّر على نص، والنص موجود في **تعليق** في نفس الملف.

### 3.4 فشل بيئي ≠ فشل حقيقي
`functions/index.js` · `functions/giftCredit.js` · `echarpe-push/index.js`
**مش في الريبو عمدًا** (فلوس وأسرار — عند المالك محليًا).
الاختبار اللي بيقراهم يعمل skip نظيف مش crash:
```js
if(!fs.existsSync(p)){ console.log('  ⏭️  الملف مش موجود — اتخطى'); return; }
```

---

## 📦 4. شكل التسليم

### ❌ متبعتش استبدال كامل للريبو
في v564 بعت نسخة كاملة **ممتازة** — الكود كان سليم 100% وبنيت فوق شغل
Claude واحترمت كل حاجة. **بس** `tests/run.js` كان النسخة القديمة،
فرجع باج كان متصلّح، والهارنس رجع أعمى.

**الكود كان صح. الضرر جه من ملف واحد راجع لنسخة أقدم.**

**القاعدة:**
- سلّم **الملفات المتغيّرة بس**
- لو لازم نسخة كاملة: **قارنها بالموجود** واعمل قايمة صريحة بكل ملف
  هيرجع لنسخة أقدم
- قبل تسليم أي ملف: `grep` على ميزة معروفة إنها موجودة فيه — لو مش لاقيها،
  الملف من فرع أقدم

### ✅ اللي المالك عايزه
- **ملفات كاملة** مش patches — هو مش هيلصق أجزاء
- **zip بهيكل فولدرات حقيقي** (`sales/sw.js`, `tests/x.js`)
- **الأسماء الحقيقية.** لو ملفين بنفس الاسم (`sw.js` بتاع sales وبتاع POS)
  → **رسالتين منفصلتين**، مش إعادة تسمية
- **دفعة واحدة → تأكيد → اللي بعدها**
- **قوله الملف بيروح فين بالظبط.** الفرق بين "ده يروح GitHub" و"ده يتشغّل
  على كمبيوتر الفرع كأدمن" **مش واضح تلقائيًا** — وده لخبطه فعلًا

---

## ⚡ 5. قواعد الكود المثبّتة (متكسرهاش)

### JavaScript
- **`const`/`let` مبتعدّيش بين بلوكات `<script>`.**
  `sales/index.html` فيه أكتر من بلوك → `window.x = x` بعد التعريف.
  الباج ده ضرب مرتين (آخرها `swapHoursFrom` كانت هتقع بصمت).
- **`prompt()` و`confirm()` بيفشلوا بصمت في Electron.**
  في `pos/` بس استخدم `askText()` / `askConfirm()`. (`sales` معفي — قرار المالك.)
- **مفيش `innerHTML` في واجهات الأخطاء.**

### PowerShell (ملفات الفروع)
- **ASCII بس — حتى التعليقات.** العربي بيكسر البارسر (ده كان إصلاح v556).
- **أولوية `-replace`:** لازم أقواس حوالين نص الاستبدال لو فيه `+`:
  ```powershell
  $x = $x -replace ('a'+$v+'b'), ('c'+$v+'d')   # ✅ من غير الأقواس بيقع
  ```
  ده وقّف تركيب v564 بالكامل:
  `The -ireplace operator allows only two elements to follow it, not 4.`
- **"صحّي" ≠ "جاهزة":** أي فحص بعد "الخدمة اشتغلت" لازم يكون **loop بمهلة**
  مش ضربة واحدة. `/health` بيقول سليم بمجرد إن ffmpeg شغال حتى قبل أول segment.

### Firestore
- **القواعد بتتجمع بـOR** — قاعدة خاصة مبتلغيش قاعدة عامة فوقها.
- **اطلب القواعد الحالية من الـConsole** قبل أي تسليم قواعد.
- **`isStaff()` = دخول بإيميل وباسورد بس.** الدخول المجهول = مرفوض.
- **الكتابات المالية ذرّية** — نقط الموظفة + العميلة + المخزون في نفس الـbatch.
- **كل استعلام له نافذة زمنية.** القراءات المفتوحة بتكلّف فلوس فعلية.

### التوقيت
- كل الحسابات على **`Africa/Cairo`** (المالك بيدخل من برّه مصر أحيانًا).
- "النهاردة" في POS = **يوم الشغل** (`bizDayStartMs`) مش اليوم التقويمي.
- مفاتيح الشهر **محلية مش UTC**.

---

## ✅ 6. تشيك ليست قبل أي تسليم

- [ ] `node tests/run.js` — عدد الملفات اللي اشتغلت == اللي في `tests/`؟
- [ ] `tests/run.js` مش في الـzip (إلا لو التعديل فيه)؟ ولو موجود، الحارس جواه؟
- [ ] لو لمست `sales-app.js`: رفعت **`SALES_BUILD` + `CACHE_NAME` + `?v=`** التلاتة؟
- [ ] لو لمست أي تطبيق تاني: رفعت `CACHE_NAME` في `sw.js` بتاعه؟
- [ ] فيه اختبار سلبي لكل إصلاح، **ومجرّب إنه بيقع فعلًا**؟
- [ ] مفيش اختبار بيثبّت رقم نسخة أو سطر تنفيذي بيمنع إصلاح؟
- [ ] لو غيّرت اسم (متغير/دالة/قيمة بتتبعت بين نظامين): عملت `grep` على
      الاسم القديم في **الريبو كله**؟
- [ ] ملفات `.ps1` كلها ASCII؟
- [ ] بتسلّم ملفات متغيّرة بس — مش استبدال كامل بيرجّع شغل حد تاني؟
- [ ] كل ملف واضح بيروح فين (GitHub / كمبيوتر فرع / Firebase Console)؟

---

## 📊 7. الحالة الحالية (v604)

```
node tests/run.js
→ 192 ملف · 8020 ناجح · 205 فاشل
```

الـ205 فشل **كلهم قديمين** من جلسات سابقة (رواتب، شات، هدايا، توظيف، tryon).
كانوا **مستخبيين** ورا موت الهارنس عند الملف 109 — مش جداد ومش من v564.
لسه محتاجين مراجعة، بس مفيش حاجة فيهم بتزيد بالتحديثات الأخيرة.

**مفتوح ومستني قرار المالك:**
- تحصين Office (باسورد الفرع نص صريح في localStorage)
- زرار خصم الغياب (إضافة ولا بديل؟)
- فاز 3 أمنيًا: نقاط الشراء server-side
- فصل مخزون البراندين (`barcode + '__' + branch`) لو Glow هتشتغل جنب echarpe
