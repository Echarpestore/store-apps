# AI_HANDOFF-15 — ١٨ أغسطس ٢٠٢٦

> يكمّل AI_HANDOFF-14. المرجع الوحيد للحالة دلوقتي.
> **الاختبارات: ٨٠٧٧ ناجح · ١٦ فاشل** (كل الـ١٦ تاريخيين، اتأكد منهم
> واحد واحد ضد نسخة الريبو الأصلية قبل أي تعديل — صفر ريجريشن).

---

## ٠) ملخص الجلسة دي — ميزة "البندانة في الشات" من الصفر لحد شغّالة

البداية: طلب المالك "عايز اشغل ميزة البندانة في الشات". اتضح إن:
- الباك إند (`functions/hijabTryOn.js`) كان مبني من جلسة سابقة، بيقبل
  `withBandana` + `bandanaColors` ويرجع شبكة صور بكل الألوان.
- الفرونت إند (`tryon/photo.html`) ما كانش عارف يستقبل/يعرض النتيجة دي.
- جانب البايعة (`pos/chat.js` / `chat-staff-ui.js`) ما كانش بيبعت
  ألوان ولا باركود بندانة خالص.

اتبنى المسار كامل من الصفر، اتكسر مرتين في الاستخدام الحقيقي، واتصلح.
التفاصيل تحت.

---

## ١) 🔴 لازم يتعمل — بالترتيب

| # | الحاجة | ليه دلوقتي |
|---|---|---|
| ١ | رفع كل الملفات المعدّلة (القايمة في §٨) على GitHub | كل حاجة اتعملت النهارده لسه محلية |
| ٢ | **تجربة حقيقية للتقسيم الرياضي الجديد** (§٣) بعد الرفع | الكشف بالتباين فشل مرة على الأقل في الواقع؛ الفولباك جديد ومتاختبرش على صورة Gemini حقيقية لسه |
| ٣ | إضافة باركود حقيقي للبندانتين الجاهزتين (`bandana-breathe`, `bandana-farah`) في `tryon/assets/catalog.js` — ده لمسار **الوضع الحي (Beta)** بس، مش شرط لمسار الشات |
| ٤ | تجربة الميزة كاملة إند تو إند من موبايل حقيقي: بايعة تبعت بندانة من الشات (POS أو sales أو Office) → عميلة تدوس جرّبيها → تختار لون → تضيف للسلة |

---

## ٢) 🧢 تجربة البندانة — البنيان الكامل (خط الأنابيب من أوله لآخره)

### أ) الباك إند — `functions/hijabTryOn.js` (منشور، ماتلمسش من غير سبب قوي)
- بيقبل `withBandana:true` + `bandanaColors:[...]` (٢-٦ لون، إنجليزي بس)
- البرومبت بيطلب من Gemini شبكة `gh × gw` (بيتحسب من عدد الألوان:
  `gw = n<=2?2:(n<=4?2:3)`, `gh = ceil(n/gw)`) — **نفس الحسبة دي**
  اتكررت في `tryon/photo-core.js` (`computeGridLayout`) عشان الفرونت
  يعرف يقص بنفس الأبعاد.
- بيرجّع `bandanaColors` بترتيبها مع الصورة — **الترتيب هو الرابط
  الوحيد بين الخانة واللون**. أي تغيير في ترتيب الإرسال (الشات →
  sessionStorage → الدالة) لازم يتحافظ عليه في كل حتة.

### ب) جانب البايعة — `pos/chat-staff-ui.js` (ملف **مشترك** بين POS/sales/Office)
- شيك بوكس "🧢 بندانة" بيظهر لما تختار صورة منتج
- **لوحة أزرار ألوان جاهزة** (`CC_BAND_COLORS`, ١٧ لون) — بديل الكتابة
  اليدوي اللي كان مزعج (المالك اشتكى منها صراحة). كل لون: قيمة
  إنجليزي (`v`, بترتيب الضغط) + لابل عربي (`l`) + hex للعرض.
  ⚠️ لو حبيت تضيف لون جديد، ضيفه هنا **وفي** `COLOR_HEX` بتاعة
  `tryon/photo-core.js` — القايمتين لازم يفضلوا متطابقين.
- حقل باركود البندانة **بيتفحص فعليًا** ضد المخزون الحقيقي (نفس
  آلية معاينة باركود المنتج الأساسي: `findByBarcode` محلي أو
  `CDB.getProduct` لو مش POS). **الإرسال بيتوقف تمامًا** لو:
  - أقل من ٢ لون متختار
  - الباركود فاضي
  - الباركود ده مش بيرجّع صنف حقيقي من المخزون
  (المالك طلب صراحة: "اتأكد إن كود البندانة شغال وبيروح منتج")
- `ccImgClear()` و`ccBandToggle()` بينضّفوا كل بيانات البندانة —
  منع تسرّب باركود/ألوان قديمة لصورة تانية.
- **الملف ده بيتحمّل من `sales/index.html` و`Office/index.html`
  بنفس المسار (`../pos/chat-staff-ui.js`)** — يعني أي تحديث هنا
  بيوصلهم أوتوماتيك، مفيش نسخة تانية تتنسى.

### ج) الرسالة في Firestore
حقول جديدة على مستند الرسالة (`customer_chat/{phone}/messages/{id}`):
`bandanaColors: string[]` (إنجليزي، ٢-٦) و`bandanaPid: string`
(باركود منفصل عن باركود الطرحة نفسها).

### د) العميلة تستقبل — `loyalty/index.html` / `glow/index.html`
- `chatBandanaColors[msgId]` / `chatBandanaPid[msgId]` بيتملوا من
  الرسالة (نفس تنضيف الألوان بالظبط: حروف بس، سقف ٦)
- `chatTryOn(id)` بيحطهم في `sessionStorage`:
  `echarpe_tryon_bandana_colors` (JSON array) +
  `echarpe_tryon_bandana_pid`
- `tryonAddToCart(barcode, imgFallback, note)` — بارامتر `note`
  جديد، بيتحط في `_shopInfo.notes` (ملاحظات الأوردر). بنستخدمه
  عشان نبعت لون البندانة المختار مع الطلب **من غير أي تعديل في
  سكيمة الصنف أو `onlineOrderPlace`** (أبسط وأأمن).
- فولباك `?addcart=<باركود>&note=<اللون>` بيتقرا عند التحميل لو
  `window.opener` مش متاح.

### هـ) صفحة التجربة — `tryon/photo.html` + `tryon/photo-core.js`
- بتقرا الألوان/الباركود من `sessionStorage`، وتطلب وضع الشبكة من
  `hijabTryOn` لو ٢ لون فأكتر
- **القص** (§٣ تحت — ده الجزء اللي اتكسر واتصلح النهارده)
- صف دواير ألوان قابل للدوس — كل دوسة بتقص وتعرض لون تاني **محليًا
  ومجانًا** (مفيش نداء شبكة تاني)
- زرار "🧢 ضيفي البندانة للسلة" منفصل عن زرار الطرحة، بيبعت اللون
  المختار كملاحظة

---

## ٣) 🔴🔴 باجين اتكسروا في تجربة حقيقية واتصلحوا النهارده

المالك بعت سكرين شوت من تجربة حقيقية وقال "باظت النتيجة... يطلع هبل".
اكتشفنا اتنين مشكلة **مختلفين تمامًا** في نفس الصورة:

### باج ١: `<button>` جوه `<button>` — HTML باطل
`moreBtn` (زرار "المزيد") كان فيه `<div class="moreMenu">` جواه فيها
`<button id="backBtn">`. **الـHTML مايسمحش بـbutton جوه button** —
المتصفح بيصلّح الشجرة بنفسه وقت التحليل، وده كان بيكسر التخطيط:
"رجوع للشات" كان بيظهر كصف دايم مكشوف بدل قايمة مخفية، وصف الأيقونات
كله بيبان "هبل" (٥ عناصر متلغبطة بدل ٤ منظمين).

**الإصلاح:** فصلت `moreBtn` و`moreMenu` كإخوة (siblings) جوه
`<div class="iconActWrap">` (بدل ما تكون `moreMenu` جوه الزرار نفسه)،
وحطيت `position:relative` على الـwrapper مش على الزرار.

**اتفحص سلبيًا:** رجّعت الباج (حطيت moreMenu جوه الزرار تاني) →
اختبار جديد وقع فورًا (`test-tryon-photo.js`: "moreBtn بيقفل قبل أي
button تاني"). استرجعت الإصلاح → نجح.

### باج ٢: كشف فواصل الشبكة فشل على صورة Gemini حقيقية
`GridSplit.splitGrid` (كشف بالتباين — بيدوّر على صفوف/أعمدة موحّدة
اللون كفواصل) **فشل** على الصورة الحقيقية اللي المالك جرّبها — يبدو
إن Gemini مارسمش فواصل واضحة كفاية بين الخانات. لما الكشف بيفشل،
الكود القديم كان بيرجّع فولباك "آمن" (يسيب الصورة كاملة زي ما هي من
غير قص) — لكن ده كان بيبان "هبل" فعليًا: العميلة تشوف ٤ وشوش صغيرة
مع بعض بدل صورة واحدة واضحة بزرار تبديل لون.

**الإصلاح — خطة هجينة (`trySplitGrid` في `photo.html`):**
1. جرّب الكشف الدقيق بالتباين الأول (لو الصورة فيها فواصل واضحة،
   هيكون أدق قص)
2. **لو فشل**، ارجع لتقسيم رياضي مضمون (`sliceGridProportional`
   في `photo-core.js`): بيقسم الصورة بالتساوي على نفس أبعاد الشبكة
   اللي اتطلبت في البرومبت (`computeGridLayout` — نفس حسبة
   `hijabTryOn.js` بالظبط: `gw=n<=2?2:(n<=4?2:3)`, `gh=ceil(n/gw)`)،
   مع `inset` صغير (٣.٥٪) ياخد مسافة أمان من حواف الخانة.
3. **النتيجة: العميلة دايمًا تشوف قص واحد قابل لتبديل اللون — مفيش
   مسار بيرجّع الشبكة الخام تاني.**

**اتفحص سلبيًا:** رجّعت الفولباك القديم (`hideBandRow()` عند فشل
الكشف بدل التقسيم الرياضي) → اختبار جديد وقع فورًا. استرجعت → نجح.

**⚠️ مش متأكدين ١٠٠٪ لسه:** التقسيم الرياضي **مفتترضش** إن الموديل
التزم بترتيب الشبكة اللي اتطلب في البرومبت. لو الموديل مبعّد الخانات
عن بعض بشكل غير متساوي (مش نص/نص بالظبط)، ممكن القص ياخد جزء من
الخانة الجنبية أو يقطع حتة من الوش عند الحافة. الـ`inset` (٣.٥٪)
بيدّي مسافة أمان، بس **التجربة الحقيقية بعد الرفع هي الحكم النهائي**
(§١ البند ٢).

---

## ٤) 📂 الملفات المعدّلة النهارده (كلها في الريبو المرفق كامل)

| الملف | التغيير |
|---|---|
| `functions/hijabTryOn.js` | **متغيّرش** — موجود هنا للمرجعية بس (مش في الريبو الأصلي، من رفعة سابقة) |
| `pos/chat-staff-ui.js` | لوحة أزرار ألوان بدل كتابة يدوي + تحقق باركود حقيقي + مشترك مع sales/Office |
| `pos/sw.js` | v328 |
| `tryon/photo.html` | تصميم جديد كامل (هيدر/كارت/شارات/صف أيقونات) + إصلاح باج button-in-button + خطة القص الهجينة |
| `tryon/photo-core.js` | دوال البندانة كلها (تنضيف/كاش بالألوان) + `computeGridLayout` + `sliceGridProportional` |
| `tryon/sw.js` | v49 |
| `tryon/tryon-app.js` | `TRYON_VER` v49 (لازم يفضل مطابق لـ`tryon/sw.js`) |
| `loyalty/index.html` | استقبال بندانة من الشات + `tryonAddToCart` بـ`note` |
| `loyalty/sw.js` | v73 |
| `glow/index.html` | نفس تعديلات loyalty |
| `glow/sw.js` | v67 |
| `tests/test-tryon-photo.js` | ~١٥٠ فحص جديد (منطق نقي + ربط الصفحة + الباجين اللي اتصلحوا) |
| `tests/test-chat-cart-visibility.js` | فحوصات لوحة الألوان + تحقق الباركود + مشاركة sales/Office للملف |
| `tests/test-tryon-cart-customer.js` | تحديث regex لتوقيع `tryonAddToCart` الجديد (بارامتر `note`) |
| `tests/test-cart-quickcheckout.js` | نفس تحديث الـregex |

---

## ٥) ⚠️ أشياء لازم تتراعى في أي شغل جاي على البندانة

1. **الترتيب مقدّس.** أي حتة في الأنابيب (لوحة أزرار البايعة →
   الرسالة → sessionStorage → البرومبت → القص → صف الدواير عند
   العميلة) لازم تحافظ على **نفس ترتيب الألوان**. لو اتغيّر في حتة
   واحدة من غير الباقي، اللون هيتحط في خانة غلط.
2. **القيم لازم تفضل إنجليزي بس.** برومبت Gemini والتنضيف
   (`[^a-zA-Z \-]`) في كل الملفات بيرفضوا أي حاجة مش حروف إنجليزي.
   لو ضفت لون جديد في `CC_BAND_COLORS` (chat-staff-ui.js)، لازم
   يتضاف بنفس القيمة الإنجليزي في `COLOR_HEX` (photo-core.js).
3. **حد ٦ ألوان** مكرر في ٣ أماكن (backend، chat-staff-ui، الشات
   في loyalty/glow) — أي تغيير في السقف لازم يتغيّر في التلاتة.
4. **الفولباك الرياضي (`sliceGridProportional`) لسه محتاج تجربة
   ميدانية** بعد الرفع — راجع §٣ باج ٢.
5. **باركود البندانة لازم صنف حقيقي في المخزون** — الإرسال بيترفض
   لو الباركود مش موجود، ده مقصود ومطلوب من المالك.

---

## ٦) 🧭 من هاندوف ١٤ (لسه مفتوح، ماتلمسش النهارده)

- 🔴 قراءات Firestore (٥٧٣ألف/يوم) — تحقيق مفتوح
- الموقع الرئيسي — سؤال الخصوصية معلّق
- رد ٣٥٠ج لعميلة فاتورة 1444 (يدوي من Paymob)
- قرار الموقع (echarpe.store) وقرار المقاسات — مستنيين رد المالك
- باركود البندانتين الجاهزتين (`bandana-breathe`, `bandana-farah`)
  في `tryon/assets/catalog.js` لسه فاضي (مسار الوضع الحي Beta بس)
- التارجت اليومي — قرار الحافز التراكمي لسه مفتوح

---

## ٧) 🧪 الاختبارات

```
node tests/run.js
```
**٨٠٧٧ ناجح · ١٦ فاشل.** الـ١٦ كلهم اتأكد منهم فرد فرد إنهم موجودين
حتى في نسخة الريبو **قبل** أي تعديل النهارده (فحص مقارنة مباشر) —
معظمهم ملفات ناقصة (`functions/giftCredit.js` مش موجود عندي) أو
فحوصات جامدة قديمة (نسخة كاش POS v319 مقابل v328 الفعلية) أو ملفات
يتيمة قديمة. **مفيش أي ريجريشن سببته تعديلات النهارده.**

منهجية الجلسة: **فحص سلبي فعلي لكل إصلاح** (مش بس مكتوب في التعليق) —
رجّعت الباج، اتأكدت إن الاختبار بيقع، استرجعت الإصلاح، اتأكدت إنه
بينجح. اتعمل ده ٤ مرات النهارده (توقيع tryonAddToCart، إخفاء شارة
اللون، باج button-in-button، فولباك القص الرياضي).
